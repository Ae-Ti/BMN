# Gradle JVM args - increase heap to avoid "Java heap space" during build
# Adjust as necessary for your machine (e.g., 2g, 4g)
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# Optional: enable Gradle daemon
org.gradle.daemon=true
